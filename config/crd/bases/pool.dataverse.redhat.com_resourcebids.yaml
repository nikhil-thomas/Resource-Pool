---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: resourcebids.pool.dataverse.redhat.com
spec:
  group: pool.dataverse.redhat.com
  names:
    kind: ResourceBid
    listKind: ResourceBidList
    plural: resourcebids
    shortNames:
    - rb
    - rbs
    singular: resourcebid
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.leaseName
      name: Lease
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceBid is the Schema for the resourcebids API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ResourceBid
            properties:
              leaseDuration:
                default: 1h
                description: |-
                  LeaseDuration specifies how long to hold the resource
                  Defaults to 1 hour if not specified
                type: string
              priority:
                default: 0
                description: |-
                  Priority affects assignment order when multiple claims are pending
                  Higher priority claims are assigned first
                format: int32
                type: integer
              requiredLabels:
                additionalProperties:
                  type: string
                description: |-
                  RequiredLabels specifies labels that must match on the resource
                  Used to filter which resources from the pool can be assigned
                  For example: {"tier": "standard", "region": "us-east-1"}
                type: object
              type:
                description: |-
                  Type specifies the resource provider type (e.g., "snowflake", "postgres", "aws-account")
                  This field is required and determines which provider will handle the resource
                minLength: 1
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of ResourceBid
            properties:
              acquiredAt:
                description: AcquiredAt is the timestamp when the lease was successfully
                  acquired
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the claim's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectionDetails:
                additionalProperties:
                  type: string
                description: |-
                  ConnectionDetails contains provider-specific connection information
                  This is the primary output consumed by users/CI pipelines
                  The keys and values are provider-specific (e.g., for Snowflake: accountUrl, warehouse, etc.)
                type: object
              credentialSecretRef:
                description: |-
                  CredentialSecretRef points to the Secret containing credentials
                  Secret is in the same namespace as this ResourceBid
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              expiresAt:
                description: ExpiresAt is when the lease will expire (AcquiredAt +
                  LeaseDuration)
                format: date-time
                type: string
              leaseName:
                description: |-
                  LeaseName is the name of the Kubernetes Lease object that was acquired
                  This can be used to inspect the lease directly
                type: string
              message:
                description: |-
                  Message provides human-readable status information
                  Examples: "No free resources available", "Successfully acquired resource-lease-snowflake-1"
                type: string
              phase:
                description: Phase indicates the current lifecycle phase
                enum:
                - Pending
                - Bound
                - Failed
                - Releasing
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
