apiVersion: pool.dataverse.redhat.com/v1alpha1
kind: ResourceBid
metadata:
  name: my-snowflake-bid
  namespace: default
spec:
  # Type of resource to bid for (must match a registered provider)
  type: snowflake

  # How long to hold the resource (optional, defaults to 1h)
  leaseDuration: 2h

  # Filter resources by labels (optional)
  # Only Snowflake accounts with matching labels will be assigned
  requiredLabels:
    tier: standard
    region: us-east-1

  # Priority for assignment (optional, defaults to 0)
  # Higher priority bids are assigned first when multiple are pending
  priority: 0
